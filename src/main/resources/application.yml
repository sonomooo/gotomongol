spring:
  application:
    name: mongol-tour
  datasource:
    url: jdbc:h2:mem:mongoltour
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8080
